fun assert(cond: bool; exit_code: usize) {
  if (!cond) {
    sys::syscall(60, exit_code);
  }
}

fun print(msg: char^; len: usize) {
  sys::syscall(1, 1, sys::cast<usize>(msg), len);
}

fun main() {
  print("Hello World!\n", 13);
  sys::syscall(60, 0);
}
